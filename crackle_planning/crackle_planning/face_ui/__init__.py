"""Whimsical web face UI for Crackle.

A tiny stdlib-only HTTP + Server-Sent-Events server (`server.py`) serves an
animated face (`index.html`) and accepts state updates from the planner FSM via
`ui_client.py`. No ROS, no build step, no extra dependencies.
"""
